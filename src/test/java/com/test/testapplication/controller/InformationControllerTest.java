package com.test.testapplication.controller;

import org.junit.Before;

//@RunWith(MockitoJUnitRunner.class)
public class InformationControllerTest {

    public static final String CODE_MESSAGE = "response should contains code";
    public static final String DESCRIPTION_MESSAGE = "response should contains description";

//    private InformationController informationController;

    @Before
    public void setUp() throws Exception {
//        informationController = new InformationController();

    }

//    @Test
//    public void handleInformation_shouldReturnInfoDetails_whenACodeIsProvided() throws Exception {
//        String country = "Fiji";
//        String description = "population";
//        InfoDetailsDTO infoDetails = new InfoDetailsDTO();
////        BDDMockito.given(restTemplate.getForObject(Mockito.anyString(), InfoDetailsDTO.class)).willReturn(infoDetails);
//        ResponseEntity<InfoDetailsDTO> response = new InformationController().handleInformation(country, description);
//
//        Assert.assertEquals(CODE_MESSAGE, response.getBody().getCode(), "BE");
//        Assert.assertEquals(DESCRIPTION_MESSAGE, response.getBody().getDescription(), "any");
//    }

}